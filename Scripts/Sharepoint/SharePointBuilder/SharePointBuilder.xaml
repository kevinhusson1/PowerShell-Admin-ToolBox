<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="##loc:sp_builder.window_title##" TextOptions.TextFormattingMode="Display" Height="980" Width="1500" WindowStartupLocation="CenterScreen" UseLayoutRounding="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- 0: En-tête + Menu -->
            <RowDefinition Height="*"/>
            <!-- 1: Contenu -->
            <RowDefinition Height="Auto"/>
            <!-- 2: Pied de page -->
        </Grid.RowDefinitions>

        <!-- =============================================================== -->
        <!-- 1. EN-TÊTE STANDARD + MENU INTÉGRÉ -->
        <!-- =============================================================== -->
        <Border Grid.Row="0" Background="{DynamicResource PrimaryBrush}" Padding="20">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Icône -->
                <Border Grid.Column="0" Width="56" Height="56" CornerRadius="20" BorderBrush="{DynamicResource WhiteBrush}" BorderThickness="2" Background="{DynamicResource PrimaryLightBrush}">
                    <Rectangle Fill="{DynamicResource WhiteBrush}" Margin="14">
                        <Rectangle.OpacityMask>
                            <ImageBrush x:Name="HeaderIconBrush" Stretch="Uniform"/>
                        </Rectangle.OpacityMask>
                    </Rectangle>
                </Border>

                <!-- Textes -->
                <StackPanel Grid.Column="1" Margin="20,0,0,0" VerticalAlignment="Center">
                    <TextBlock x:Name="HeaderTitleText" Text="##loc:scripts.DefaultUI.name##" Foreground="{DynamicResource WhiteBrush}" FontSize="24" FontWeight="SemiBold"/>
                    <TextBlock x:Name="HeaderSubtitleText" Text="##loc:scripts.DefaultUI.description##" Foreground="{DynamicResource WhiteBrush}" Opacity="0.8" FontSize="14"/>
                </StackPanel>

                <!-- Auth -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">

                    <!-- Nom / État (Texte) -->
                    <Button x:Name="ScriptAuthTextButton" Content="Non connecté" Margin="0,0,10,0" VerticalAlignment="Center" Cursor="Arrow" FontSize="13" FontWeight="SemiBold">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Foreground" Value="{DynamicResource WhiteBrush}"/>
                                <!-- Blanc sur fond bleu -->
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}">
                                                <ContentPresenter VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>

                    <!-- Macaron (Cercle) -->
                    <Button x:Name="ScriptAuthStatusButton" Style="{DynamicResource ProfileButtonStyle}" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
        <!-- Ligne 2 : Menu TabControl -->
        <Grid Grid.Row="1" Background="{DynamicResource PrimaryBrush}">
            <TabControl x:Name="MainTabControl" Style="{DynamicResource NavTabControlStyle}">
                <!-- ONGLET DÉPLOIEMENT -->
                <TabItem Header="🚀 Déploiement" IsSelected="True">
                    <Grid Background="{DynamicResource WhiteBrush}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="600" MinWidth="400"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" Margin="25,25,8,25">
                                <!-- COLONNE GAUCHE (CONFIG) -->
                                <StackPanel>
                                    <!-- CARTE 1 : CIBLE -->
                                    <Border Style="{DynamicResource CardBorderStyle}">
                                        <StackPanel>
                                            <Grid Margin="0,0,0,15">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <Border Style="{DynamicResource StepIconBorderStyle}" Background="{DynamicResource InfoBackgroundBrush}">
                                                    <TextBlock Text="1" Foreground="{DynamicResource InfoBrush}" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <TextBlock Text="Cible du Déploiement" Style="{DynamicResource StepHeaderTextStyle}" Grid.Column="1"/>
                                            </Grid>

                                            <TextBlock Text="Site SharePoint" Style="{DynamicResource H4Style}"/>
                                            <ComboBox x:Name="SiteComboBox" Style="{DynamicResource StandardEditableComboBoxStyle}" Margin="0,5,0,15"/>

                                            <TextBlock Text="Bibliothèque de Documents" Style="{DynamicResource H4Style}"/>
                                            <ComboBox x:Name="LibraryComboBox" Style="{DynamicResource StandardComboBoxStyle}"/>
                                        </StackPanel>
                                    </Border>

                                    <!-- CARTE 3 : CONFIGURATION DOSSIER -->
                                    <Border Style="{DynamicResource CardBorderStyle}">
                                        <StackPanel>
                                            <Grid Margin="0,0,0,15">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Orientation="Horizontal">
                                                    <Border Style="{DynamicResource StepIconBorderStyle}" Background="{DynamicResource WarningBackgroundBrush}">
                                                        <TextBlock Text="3" Foreground="{DynamicResource WarningBrush}" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <TextBlock Text="Création de la Destination" Style="{DynamicResource StepHeaderTextStyle}"/>
                                                </StackPanel>
                                                <Border Grid.Column="2" Background="{DynamicResource BackgroundMediumBrush}" CornerRadius="4" Padding="6,2" VerticalAlignment="Center" Margin="0,0,0,15">
                                                    <TextBlock Text="Optionnel" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                                </Border>
                                            </Grid>

                                            <!-- Toggle + Choix Modèle Dossier -->
                                            <Grid Margin="0,0,0,20">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <CheckBox Grid.Column="0" x:Name="CreateFolderCheckBox" Style="{DynamicResource ToggleSwitchStyle}"/>
                                                <TextBlock Grid.Column="1" Text="Créer un dossier de destination" VerticalAlignment="Center" Margin="10,0,0,0" FontWeight="SemiBold"/>
                                                <ComboBox Grid.Column="2" x:Name="FolderTemplateComboBox" Style="{DynamicResource StandardComboBoxStyle}" IsEnabled="{Binding IsChecked, ElementName=CreateFolderCheckBox}"/>
                                            </Grid>

                                            <!-- Formulaire Dynamique -->
                                            <StackPanel x:Name="DynamicFormPanel" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,15"/>

                                            <!-- Preview Résultat -->
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="Résultat :  →" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,10,0"/>
                                                <Border Grid.Column="1" Background="{DynamicResource BackgroundMediumBrush}" CornerRadius="6" Padding="12,8" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="1">
                                                    <TextBlock x:Name="FolderNamePreviewText" Text="..." FontFamily="Consolas" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="13"/>
                                                </Border>
                                            </Grid>
                                            <TextBlock Text="Format standard défini par la règle de nommage." FontSize="10" FontStyle="Italic" Foreground="{DynamicResource TextDisabledBrush}" Margin="0,5,0,0" HorizontalAlignment="Right"/>
                                        </StackPanel>
                                    </Border>

                                    <!-- CARTE OPTIONS -->
                                    <Border Style="{DynamicResource CardBorderStyle}">
                                        <StackPanel>
                                            <Grid Margin="0,0,0,10">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <Border Style="{DynamicResource StepIconBorderStyle}" Background="{DynamicResource SecondaryBackgroundBrush}">
                                                    <TextBlock Text="⚙" Foreground="{DynamicResource SecondaryBrush}" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16"/>
                                                </Border>
                                                <TextBlock Text="Options Avancées" Style="{DynamicResource StepHeaderTextStyle}" Grid.Column="1"/>
                                            </Grid>

                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                                    <CheckBox x:Name="OverwritePermissionsCheckBox" Style="{DynamicResource ToggleSwitchStyle}"/>
                                                    <TextBlock Text="Écraser les permissions existantes" Margin="10,0,0,0" VerticalAlignment="Center"/>
                                                </StackPanel>

                                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                                    <Button x:Name="ResetUIButton" Content="↺ Réinitialiser" Style="{DynamicResource SecondaryButtonStyle}" Height="32" FontSize="11" Margin="0,0,10,0"/>
                                                    <Button x:Name="ExportConfigButton" Content="↗ Exporter" Style="{DynamicResource PrimaryButtonStyle}" Height="32" FontSize="11"/>
                                                </StackPanel>
                                            </Grid>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </ScrollViewer>

                            <GridSplitter Style="{DynamicResource ModernGridSplitterStyle}" Grid.Column="1" Width="5" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="Transparent" ResizeBehavior="PreviousAndNext"/>

                            <Grid Grid.Column="2" Margin="8,25,25,25">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="250"/>
                                </Grid.RowDefinitions>
                                <!-- CARTE 2 : ARBORESCENCE -->
                                <Border Grid.Row="0" Style="{DynamicResource CardBorderStyle}">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>

                                        <!-- Header Interne -->
                                        <Border Grid.Row="0">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <!-- Icone -->
                                                    <ColumnDefinition Width="*"/>
                                                    <!-- Titre -->
                                                    <ColumnDefinition Width="*"/>
                                                    <!-- ComboBox -->
                                                    <ColumnDefinition Width="Auto"/>
                                                    <!-- Info -->
                                                </Grid.ColumnDefinitions>

                                                <Border Grid.Column="0" Style="{DynamicResource StepIconBorderStyle}" Background="{DynamicResource SecondaryBackgroundBrush}">
                                                    <TextBlock Text="2" Foreground="{DynamicResource InfoBrush}" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <TextBlock Grid.Column="1" Text="Modèle à Appliquer" Style="{DynamicResource StepHeaderTextStyle}"/>

                                                <ComboBox Grid.Column="2" x:Name="TemplateComboBox" Style="{DynamicResource StandardComboBoxStyle}" Height="32"/>

                                                <TextBlock Grid.Column="3" x:Name="TemplateDescText" Text="ℹ️" ToolTip="Sélectionnez un modèle pour voir sa description" Margin="10,0,0,0" VerticalAlignment="Center" Cursor="Help"/>
                                            </Grid>
                                        </Border>
                                        <!-- TreeView -->
                                        <Border Grid.Row="1" Margin="15" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="1" CornerRadius="4" Background="{DynamicResource WhiteBrush}">
                                            <TreeView x:Name="PreviewTreeView" Style="{DynamicResource ModernTreeViewStyle}" BorderThickness="0"/>
                                        </Border>

                                    </Grid>
                                </Border>

                                <!-- CARTE LOGS -->
                                <Border Grid.Row="1" Style="{DynamicResource CardBorderStyle}" Padding="0" ClipToBounds="True">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>

                                        <!-- Header Logs -->
                                        <Border Grid.Row="0" Background="{DynamicResource BackgroundLightBrush}" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="0,0,0,1" Padding="15,8">
                                            <Grid>
                                                <TextBlock Text="Journal des Opérations" FontWeight="SemiBold" VerticalAlignment="Center" Margin="5,0,0,0"/>
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                                    <TextBlock Text="Niveau :" FontSize="11" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                                    <ComboBox x:Name="LogLevelComboBox" Width="80" Height="24" FontSize="11" SelectedIndex="1" Margin="0,0,10,0">
                                                        <ComboBoxItem Content="Light" Tag="Light"/>
                                                        <ComboBoxItem Content="Normal" Tag="Normal"/>
                                                        <ComboBoxItem Content="Expert" Tag="Hard"/>
                                                    </ComboBox>
                                                    <Ellipse Width="10" Height="10" Fill="#FF5F56" Margin="0,0,6,0"/>
                                                    <Ellipse Width="10" Height="10" Fill="#FFBD2E" Margin="0,0,6,0"/>
                                                    <Ellipse Width="10" Height="10" Fill="#27C93F"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>

                                        <RichTextBox Grid.Row="1" x:Name="LogRichTextBox" Style="{DynamicResource StandardRichTextBoxStyle}" BorderThickness="0" Background="{DynamicResource WhiteBrush}" FontFamily="Consolas" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                    </Grid>
                                </Border>
                            </Grid>
                        </Grid>
                    </Grid>
                </TabItem>

                <!-- PLACEHOLDERS -->
                <!-- ONGLET 2 : ÉDITEUR DE MODÈLE -->
                <TabItem Header="✏️ Éditeur de Modèle">
                    <Grid Background="{DynamicResource BackgroundLightBrush}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <!-- Toolbar -->
                            <RowDefinition Height="*"/>
                            <!-- Workspace -->
                        </Grid.RowDefinitions>

                        <!-- 1. BARRE D'OUTILS & CONTEXTE -->
                        <Border Grid.Row="0" Background="{DynamicResource WhiteBrush}" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="0,0,0,1" Padding="20,15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <!-- Gestion Fichier -->
                                    <ColumnDefinition Width="*"/>
                                    <!-- Espace -->
                                    <ColumnDefinition Width="Auto"/>
                                    <!-- Validation Context -->
                                    <ColumnDefinition Width="20"/>
                                    <!-- Espace -->
                                    <ColumnDefinition Width="Auto"/>
                                    <!-- Actions Principales -->
                                </Grid.ColumnDefinitions>

                                <!-- Gestion BDD -->
                                <StackPanel Grid.Column="0" Orientation="Horizontal">
                                    <StackPanel VerticalAlignment="Center" Margin="0,0,10,0">
                                        <TextBlock Text="Modèle existant" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                        <ComboBox x:Name="EditorLoadComboBox" Width="200" Style="{DynamicResource StandardComboBoxStyle}" Height="32"/>
                                    </StackPanel>
                                    <Button x:Name="EditorLoadButton" Content="Charger" Style="{DynamicResource SecondaryButtonStyle}" Height="32" VerticalAlignment="Bottom"/>
                                    <Button x:Name="EditorDeleteTemplateButton" Content="🗑️" Style="{DynamicResource SecondaryButtonStyle}" Height="32" Width="40" VerticalAlignment="Bottom" Margin="5,0,0,0" Foreground="{DynamicResource DangerBrush}" ToolTip="Supprimer le modèle sélectionné"/>
                                </StackPanel>

                                <!-- Contexte de Validation (IntelliSense) -->
                                <Border Grid.Column="2" Background="{DynamicResource InfoBackgroundBrush}" CornerRadius="8" Padding="10,5" BorderBrush="{DynamicResource InfoBrush}" BorderThickness="1">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="🎯" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock Text="Contexte de validation" FontSize="9" Foreground="{DynamicResource InfoHoverBrush}" FontWeight="Bold"/>
                                            <TextBlock x:Name="EditorContextStatusText" Text="Aucun site cible défini" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource InfoHoverBrush}"/>
                                        </StackPanel>
                                        <Button x:Name="EditorDefineTargetButton" Content="Définir" Style="{DynamicResource SecondaryButtonStyle}" Height="24" FontSize="10" Padding="8,0" Margin="10,0,0,0" Background="{DynamicResource WhiteBrush}"/>
                                    </StackPanel>
                                </Border>

                                <!-- Actions Globales -->
                                <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center">
                                    <Button x:Name="EditorNewButton" Content="✨ Nouveau" Style="{DynamicResource SecondaryButtonStyle}" Height="36" Margin="0,0,10,0"/>
                                    <Button x:Name="EditorSaveButton" Content="💾 Sauvegarder" Style="{DynamicResource PrimaryButtonStyle}" Height="36"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- 2. ZONE DE TRAVAIL -->
                        <Grid Grid.Row="1" Margin="25">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"/>
                                <!-- Arbre -->
                                <ColumnDefinition Width="25"/>
                                <!-- Espace -->
                                <ColumnDefinition Width="1.5*"/>
                                <!-- Propriétés -->
                            </Grid.ColumnDefinitions>

                            <!-- COLONNE GAUCHE : STRUCTURE -->
                            <Border Grid.Column="0" Style="{DynamicResource CardBorderStyle}" Padding="0" ClipToBounds="True">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Header Arbre -->
                                    <Border Grid.Row="0" Background="{DynamicResource BackgroundLightBrush}" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="0,0,0,1" Padding="15,10">
                                        <Grid>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="📂" Margin="0,0,10,0"/>
                                                <TextBlock Text="Structure du modèle" FontWeight="SemiBold"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>

                                    <!-- TreeView Éditable -->
                                    <TreeView x:Name="EditorTreeView" Grid.Row="1" Style="{DynamicResource ModernTreeViewStyle}" BorderThickness="0" Margin="5"/>

                                    <!-- Toolbar Arbre -->
                                    <Border Grid.Row="2" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="0,1,0,0" Padding="10">
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Button x:Name="EditorAddRootButton" Content="+ Racine" Style="{DynamicResource SecondaryButtonStyle}" Height="30" FontSize="11" Margin="0,0,5,0"/>
                                            <Button x:Name="EditorAddChildButton" Content="+ Dossier" Style="{DynamicResource SecondaryButtonStyle}" Height="30" FontSize="11" Margin="0,0,5,0"/>
                                            <Button x:Name="EditorDeleteNodeButton" Content="🗑️" Style="{DynamicResource SecondaryButtonStyle}" Height="30" Foreground="{DynamicResource DangerBrush}"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </Border>

                            <!-- COLONNE DROITE : INSPECTEUR -->
                            <Border Grid.Column="2" Style="{DynamicResource CardBorderStyle}" Padding="0" ClipToBounds="True">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <!-- Header Inspecteur -->
                                    <Border Grid.Row="0" Background="{DynamicResource BackgroundLightBrush}" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="0,0,0,1" Padding="15,10">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🛠️" Margin="0,0,10,0"/>
                                            <TextBlock Text="Propriétés du dossier sélectionné" FontWeight="SemiBold"/>
                                        </StackPanel>
                                    </Border>

                                    <!-- Formulaire Propriétés -->
                                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20">
                                        <StackPanel x:Name="EditorPropertiesPanel">

                                            <!-- 1. Identification -->
                                            <TextBlock Text="Nom du dossier" Style="{DynamicResource H4Style}"/>
                                            <TextBox x:Name="EditorFolderNameTextBox" Style="{DynamicResource StandardTextBoxStyle}" Margin="0,5,0,20"/>

                                            <!-- 2. Permissions -->
                                            <Grid Margin="0,0,0,10">
                                                <TextBlock Text="Permissions" Style="{DynamicResource H4Style}" VerticalAlignment="Center"/>
                                                <Button x:Name="EditorAddPermButton" Content="+" Style="{DynamicResource PrimaryButtonStyle}" Width="24" Height="24" Padding="0" HorizontalAlignment="Right" ToolTip="Ajouter une permission"/>
                                            </Grid>

                                            <!-- Liste Permissions (Template Item défini en Code ou Resources) -->
                                            <ListBox x:Name="EditorPermissionsListBox" Margin="0,0,0,20" BorderThickness="0" Background="Transparent">
                                                <!-- Style Placeholder pour le design, sera remplacé par DataTemplate -->
                                                <ListBox.ItemContainerStyle>
                                                    <Style TargetType="ListBoxItem">
                                                        <Setter Property="Padding" Value="0"/>
                                                        <Setter Property="Margin" Value="0,0,0,5"/>
                                                        <Setter Property="Background" Value="Transparent"/>
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="ListBoxItem">
                                                                    <ContentPresenter/>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </ListBox.ItemContainerStyle>
                                            </ListBox>

                                            <Separator Background="{DynamicResource BorderLightBrush}" Margin="0,0,0,20"/>

                                            <!-- 3. Métadonnées -->
                                            <Grid Margin="0,0,0,10">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="Métadonnées / Tags" Style="{DynamicResource H4Style}" VerticalAlignment="Center"/>
                                                    <!-- Badge Context -->
                                                    <Border x:Name="EditorMetadataBadge" Background="{DynamicResource InfoBackgroundBrush}" CornerRadius="4" Padding="6,2" Margin="10,0,0,0" Visibility="Collapsed">
                                                        <TextBlock Text="Schéma lié" FontSize="9" Foreground="{DynamicResource InfoBrush}" FontWeight="Bold"/>
                                                    </Border>
                                                </StackPanel>
                                                <Button x:Name="EditorAddTagButton" Content="+" Style="{DynamicResource PrimaryButtonStyle}" Width="24" Height="24" Padding="0" HorizontalAlignment="Right" ToolTip="Ajouter un tag"/>
                                            </Grid>

                                            <!-- Liste Tags -->
                                            <ListBox x:Name="EditorTagsListBox" Margin="0,0,0,20" BorderThickness="0" Background="Transparent">
                                                <ListBox.ItemContainerStyle>
                                                    <Style TargetType="ListBoxItem">
                                                        <Setter Property="Padding" Value="0"/>
                                                        <Setter Property="Margin" Value="0,0,0,5"/>
                                                        <Setter Property="Background" Value="Transparent"/>
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="ListBoxItem">
                                                                    <ContentPresenter/>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </ListBox.ItemContainerStyle>
                                            </ListBox>

                                            <!-- 4. Liens  -->
                                            <Separator Background="{DynamicResource BorderLightBrush}" Margin="0,0,0,20"/>
                                            <Grid Margin="0,0,0,10">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="Liens / Raccourcis" Style="{DynamicResource H4Style}" VerticalAlignment="Center"/>
                                                </StackPanel>
                                                <Button x:Name="EditorAddLinkButton" Content="+" Style="{DynamicResource PrimaryButtonStyle}" Width="24" Height="24" Padding="0" HorizontalAlignment="Right" ToolTip="Ajouter un lien"/>
                                            </Grid>

                                            <!-- Liste Liens -->
                                            <ListBox x:Name="EditorLinksListBox" Margin="0,0,0,20" BorderThickness="0" Background="Transparent">
                                                <ListBox.ItemContainerStyle>
                                                    <Style TargetType="ListBoxItem">
                                                        <Setter Property="Padding" Value="0"/>
                                                        <Setter Property="Margin" Value="0,0,0,5"/>
                                                        <Setter Property="Background" Value="Transparent"/>
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="ListBoxItem">
                                                                    <ContentPresenter/>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </ListBox.ItemContainerStyle>
                                            </ListBox>
                                        </StackPanel>
                                    </ScrollViewer>

                                    <!-- Overlay "Aucune sélection" -->
                                    <Grid Grid.Row="1" x:Name="EditorNoSelectionPanel" Background="{DynamicResource WhiteBrush}" Visibility="Visible">
                                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Opacity="0.5">
                                            <TextBlock Text="👆" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                                            <TextBlock Text="Sélectionnez un dossier dans l'arbre" FontSize="14"/>
                                        </StackPanel>
                                    </Grid>
                                </Grid>
                            </Border>
                        </Grid>
                    </Grid>
                </TabItem>

                <!-- ONGLET 3 : FORMULAIRE DESTINATION -->
                <TabItem Header="📋 Formulaire Destination">
                    <Grid Background="{DynamicResource BackgroundLightBrush}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <!-- Toolbar -->
                            <RowDefinition Height="*"/>
                            <!-- Workspace -->
                            <RowDefinition Height="Auto"/>
                            <!-- Preview -->
                        </Grid.RowDefinitions>

                        <!-- 1. BARRE D'OUTILS -->
                        <Border Grid.Row="0" Background="{DynamicResource WhiteBrush}" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="0,0,0,1" Padding="20,15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <!-- Gestion Fichier -->
                                    <ColumnDefinition Width="*"/>
                                    <!-- Espace -->
                                    <ColumnDefinition Width="Auto"/>
                                    <!-- Actions -->
                                </Grid.ColumnDefinitions>

                                <!-- Gestion BDD -->
                                <StackPanel Grid.Column="0" Orientation="Horizontal">
                                    <StackPanel VerticalAlignment="Center" Margin="0,0,10,0">
                                        <TextBlock Text="Règle existante" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                        <ComboBox x:Name="FormLoadComboBox" Width="200" Style="{DynamicResource StandardComboBoxStyle}" Height="32"/>
                                    </StackPanel>
                                    <Button x:Name="FormLoadButton" Content="Charger" Style="{DynamicResource SecondaryButtonStyle}" Height="32" VerticalAlignment="Bottom"/>
                                    <Button x:Name="FormDeleteButton" Content="🗑️" Style="{DynamicResource SecondaryButtonStyle}" Height="32" Width="40" VerticalAlignment="Bottom" Margin="5,0,0,0" Foreground="{DynamicResource DangerBrush}"/>
                                </StackPanel>

                                <!-- Actions -->
                                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                    <Button x:Name="FormNewButton" Content="✨ Nouveau" Style="{DynamicResource SecondaryButtonStyle}" Height="36" Margin="0,0,10,0"/>
                                    <Button x:Name="FormSaveButton" Content="💾 Sauvegarder" Style="{DynamicResource PrimaryButtonStyle}" Height="36"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- 2. ZONE DE TRAVAIL -->
                        <Grid Grid.Row="1" Margin="25">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"/>
                                <!-- Liste Champs -->
                                <ColumnDefinition Width="25"/>
                                <!-- Espace -->
                                <ColumnDefinition Width="1*"/>
                                <!-- Propriétés -->
                            </Grid.ColumnDefinitions>

                            <!-- GAUCHE : LISTE DES CHAMPS -->
                            <Border Grid.Column="0" Style="{DynamicResource CardBorderStyle}" Padding="0" ClipToBounds="True">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Header -->
                                    <Border Grid.Row="0" Background="{DynamicResource BackgroundLightBrush}" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="0,0,0,1" Padding="15,10">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="abc" FontFamily="Consolas" Margin="0,0,10,0" FontWeight="Bold" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                            <TextBlock Text="Structure du nommage" FontWeight="SemiBold"/>
                                        </StackPanel>
                                    </Border>

                                    <!-- Liste Champs -->
                                    <ListBox x:Name="FormFieldsListBox" Grid.Row="1" BorderThickness="0" Background="{DynamicResource WhiteBrush}" Padding="10">
                                        <ListBox.ItemContainerStyle>
                                            <Style TargetType="ListBoxItem">
                                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                                <Setter Property="Padding" Value="10"/>
                                                <Setter Property="Margin" Value="0,0,0,5"/>
                                                <Setter Property="Background" Value="{DynamicResource BackgroundLightBrush}"/>
                                                <Setter Property="BorderThickness" Value="1"/>
                                                <Setter Property="BorderBrush" Value="{DynamicResource BorderLightBrush}"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="ListBoxItem">
                                                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                                <ContentPresenter/>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsSelected" Value="True">
                                                                    <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                                                                    <Setter Property="Background" Value="{DynamicResource PrimaryBackgroundColor}"/>
                                                                </Trigger>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter Property="BorderBrush" Value="{DynamicResource BorderMediumBrush}"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </ListBox.ItemContainerStyle>
                                        <!-- Le Template des items sera généré en code pour plus de flexibilité -->
                                    </ListBox>

                                    <!-- Toolbar Bas -->
                                    <Border Grid.Row="2" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="0,1,0,0" Padding="10">
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Button x:Name="FormAddLabelBtn" Content="+ Texte Fixe" Style="{DynamicResource SecondaryButtonStyle}" Height="30" FontSize="11" Margin="0,0,5,0"/>
                                            <Button x:Name="FormAddTextBtn" Content="+ Champ Saisie" Style="{DynamicResource SecondaryButtonStyle}" Height="30" FontSize="11" Margin="0,0,5,0"/>
                                            <Button x:Name="FormAddComboBtn" Content="+ Liste Choix" Style="{DynamicResource SecondaryButtonStyle}" Height="30" FontSize="11" Margin="0,0,15,0"/>

                                            <!-- Correction Icônes -->
                                            <Button x:Name="FormMoveUpBtn" Content="⬆️" Style="{DynamicResource SecondaryButtonStyle}" Width="34" Height="30" Margin="0,0,5,0"/>
                                            <Button x:Name="FormMoveDownBtn" Content="⬇️" Style="{DynamicResource SecondaryButtonStyle}" Width="34" Height="30" Margin="0,0,15,0"/>

                                            <Button x:Name="FormRemoveBtn" Content="🗑️" Style="{DynamicResource IconButtonStyle}" Width="34" Height="30" Foreground="{DynamicResource DangerBrush}"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </Border>

                            <!-- DROITE : PROPRIÉTÉS -->
                            <Border Grid.Column="2" Style="{DynamicResource CardBorderStyle}" Padding="0" ClipToBounds="True">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <Border Grid.Row="0" Background="{DynamicResource BackgroundLightBrush}" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="0,0,0,1" Padding="15,10">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="⚙️" Margin="0,0,10,0"/>
                                            <TextBlock Text="Configuration du champ" FontWeight="SemiBold"/>
                                        </StackPanel>
                                    </Border>

                                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20">
                                        <StackPanel x:Name="FormPropertiesPanel">

                                            <!-- 1. Nom Interne (ENCAPSULÉ) -->
                                            <StackPanel x:Name="PanelPropName">
                                                <TextBlock Text="Nom de la variable (Interne)" Style="{DynamicResource H4Style}"/>
                                                <TextBox x:Name="PropNameBox" Style="{DynamicResource StandardTextBoxStyle}" Margin="0,5,0,15"/>
                                                <TextBlock Text="Utilisé pour l'autopilot. Ex: 'CodeChantier'" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,-10,0,20"/>
                                            </StackPanel>

                                            <!-- 2. Contenu (Label) -->
                                            <StackPanel x:Name="PanelPropContent">
                                                <TextBlock Text="Texte affiché" Style="{DynamicResource H4Style}"/>
                                                <TextBox x:Name="PropContentBox" Style="{DynamicResource StandardTextBoxStyle}" Margin="0,5,0,20"/>
                                            </StackPanel>

                                            <!-- 3. Valeur par défaut -->
                                            <StackPanel x:Name="PanelPropDefault">
                                                <TextBlock Text="Valeur par défaut" Style="{DynamicResource H4Style}"/>
                                                <TextBox x:Name="PropDefaultBox" Style="{DynamicResource StandardTextBoxStyle}" Margin="0,5,0,20"/>
                                            </StackPanel>

                                            <!-- 4. Options -->
                                            <StackPanel x:Name="PanelPropOptions">
                                                <TextBlock Text="Options (séparées par virgule)" Style="{DynamicResource H4Style}"/>
                                                <TextBox x:Name="PropOptionsBox" Style="{DynamicResource StandardTextBoxStyle}" Margin="0,5,0,20"/>
                                            </StackPanel>

                                            <!-- 5. Largeur (ENCAPSULÉ) -->
                                            <StackPanel x:Name="PanelPropWidth">
                                                <TextBlock Text="Largeur visuelle (px)" Style="{DynamicResource H4Style}"/>
                                                <TextBox x:Name="PropWidthBox" Style="{DynamicResource StandardTextBoxStyle}" Width="80" HorizontalAlignment="Left" Margin="0,5,0,20"/>
                                            </StackPanel>

                                        </StackPanel>
                                    </ScrollViewer>

                                    <!-- Overlay Vide -->
                                    <Grid Grid.Row="1" x:Name="FormNoSelectionPanel" Background="{DynamicResource WhiteBrush}" Visibility="Visible">
                                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Opacity="0.5">
                                            <TextBlock Text="👈" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                                            <TextBlock Text="Sélectionnez un champ à gauche" FontSize="14"/>
                                        </StackPanel>
                                    </Grid>
                                </Grid>
                            </Border>
                        </Grid>

                        <!-- 3. PREVIEW LIVE -->
                        <Border Grid.Row="2" Background="{DynamicResource WhiteBrush}" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="0,1,0,0" Padding="25,15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Aperçu en temps réel :" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,20,0"/>

                                <!-- Conteneur du formulaire généré -->
                                <StackPanel x:Name="FormLivePreviewPanel" Grid.Column="1" Orientation="Horizontal"/>
                            </Grid>
                        </Border>
                    </Grid>
                </TabItem>
            </TabControl>
        </Grid>

        <!-- =============================================================== -->
        <!-- 3. PIED DE PAGE (Footer) -->
        <!-- =============================================================== -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Barre de progression -->
            <ProgressBar Grid.Row="0" x:Name="MainProgressBar" Height="4" BorderThickness="0" Background="{DynamicResource BorderLightBrush}" Foreground="{DynamicResource PrimaryBrush}" VerticalAlignment="Top"/>

            <!-- Contenu Footer -->
            <Border Grid.Row="1" Background="{DynamicResource WhiteBrush}" Padding="25,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" x:Name="ProgressStatusText" Text="Prêt." FontSize="13" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,20,0"/>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <StackPanel x:Name="PostDeployActionsPanel" Orientation="Horizontal" Visibility="Visible">
                            <Button x:Name="CopyUrlButton" Content="📋 Copier URL" Style="{DynamicResource SecondaryButtonStyle}" Height="32" FontSize="12" Margin="0,0,10,0" IsEnabled="False"/>
                            <Button x:Name="OpenUrlButton" Content="↗ Ouvrir" Style="{DynamicResource SecondaryButtonStyle}" Height="32" FontSize="12" Margin="0,0,10,0" IsEnabled="False"/>
                            <Button x:Name="DeployButton" Content="🚀 Lancer le Déploiement" Style="{DynamicResource GreenButtonStyle}" Width="240" Height="40" FontSize="14" FontWeight="Bold"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

    </Grid>
</Window>