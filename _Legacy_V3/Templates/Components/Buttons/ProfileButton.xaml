<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Style TargetType="Button" x:Key="ProfileButtonStyle">
        <Setter Property="Width" Value="40"/>
        <Setter Property="Height" Value="40"/>
        <!-- L'état par défaut (Système) est défini ici. L'état "Utilisateur" est appliqué dynamiquement. -->
        <Setter Property="Background" Value="{DynamicResource AuthButtonSystemBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <!-- Le cercle de fond -->
                        <Ellipse x:Name="backgroundEllipse" Fill="{TemplateBinding Background}"/>
                        <!-- Le contenu (Texte ou Icône) -->
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <!-- Effet de survol amélioré : assombrissement subtil. -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsEnabled" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="backgroundEllipse" Property="Opacity" Value="0.85"/>
                        </MultiTrigger>
                        <!-- Effet "pressé" pour un meilleur retour utilisateur -->
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="backgroundEllipse" Property="Opacity" Value="0.7"/>
                        </Trigger>
                        <!-- État désactivé -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="backgroundEllipse" Property="Opacity" Value="0.5"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>