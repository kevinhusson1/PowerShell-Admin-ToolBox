<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="ModernGridSplitterStyle" TargetType="GridSplitter">
        <Setter Property="Width" Value="8"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="GridSplitter">
                    <!-- Zone de hit invisible (large) -->
                    <Grid Background="Transparent">

                        <!-- Ligne verticale fine (visuelle) -->
                        <Border x:Name="SplitterLine" Width="1" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="{DynamicResource BorderLightBrush}"/>

                        <!-- Poignée centrale (Grip) -->
                        <Border x:Name="SplitterGrip" Width="4" Height="24" CornerRadius="2" VerticalAlignment="Center" HorizontalAlignment="Center" Background="{DynamicResource BorderMediumBrush}"/>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <!-- Survol : On allume en Bleu Primaire -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="SplitterLine" Property="Background" Value="{DynamicResource PrimaryLightBrush}"/>
                            <Setter TargetName="SplitterGrip" Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                            <Setter Property="Cursor" Value="SizeWE"/>
                        </Trigger>

                        <!-- En cours de glissement : On fonce la couleur -->
                        <Trigger Property="IsDragging" Value="True">
                            <Setter TargetName="SplitterLine" Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                            <Setter TargetName="SplitterGrip" Property="Background" Value="{DynamicResource PrimaryHoverBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>