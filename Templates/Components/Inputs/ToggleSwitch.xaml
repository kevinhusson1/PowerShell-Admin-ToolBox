<!-- Templates/Components/Inputs/ToggleSwitch.xaml -->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="{x:Type CheckBox}" x:Key="ToggleSwitchStyle">
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CheckBox}">
                    <!-- MODIFIÉ : Taille réduite pour un aspect plus fin -->
                    <Grid Width="36" Height="20" VerticalAlignment="Center">
                        <!-- La "piste" -->
                        <Border x:Name="Track" CornerRadius="10" Background="{DynamicResource LightGrayBrush}"/>
                        
                        <!-- Le "pouce" (également réduit) -->
                        <Border x:Name="Thumb" CornerRadius="8" Width="16" Height="16" 
                                Background="White"
                                HorizontalAlignment="Left"
                                Margin="2,0,0,0">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="1" BlurRadius="2" Opacity="0.2"/>
                            </Border.Effect>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <!-- Trigger pour l'état "coché" (activé) -->
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Track" Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                            <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                            <!-- MODIFIÉ : Ajout de la marge symétrique à droite -->
                            <Setter TargetName="Thumb" Property="Margin" Value="0,0,2,0"/>
                        </Trigger>
                        
                        <!-- AJOUT : MultiTriggers pour le survol conditionnel -->
                        <!-- Cas 1 : Survol ET le switch est INACTIF -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsChecked" Value="False"/>
                                <!-- AJOUT : Condition d'activation -->
                                <Condition Property="IsEnabled" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Track" Property="Background" Value="{DynamicResource GrayBrush}"/>
                        </MultiTrigger>
                        
                        <!-- Cas 2 : Survol ET le switch est ACTIF ET ACTIVÉ -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsChecked" Value="True"/>
                                <!-- AJOUT : Condition d'activation -->
                                <Condition Property="IsEnabled" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Track" Property="Background" Value="{DynamicResource PrimaryHoverBrush}"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>