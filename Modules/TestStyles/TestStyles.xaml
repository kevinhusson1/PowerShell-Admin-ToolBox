<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ToolBox - Test Styles Personnalisés" 
        Height="400" 
        Width="500"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="🧪 Test du Système de Styles Personnalisés" Style="{DynamicResource HeaderText}" HorizontalAlignment="Center" Margin="0,0,0,20"/>
        
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Ce module teste le chargement de GlobalStyles.xaml" Style="{DynamicResource BodyText}" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                <Button x:Name="TestButton" Content="Test Standard" Style="{DynamicResource StandardButton}" Margin="0,0,10,0"/>
                <Button Content="Test Primary" Style="{DynamicResource PrimaryButton}" Margin="0,0,10,0"/>
                <Button Content="Test Success" Style="{DynamicResource SuccessButton}"/>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                <TextBox Text="Test TextBox" Style="{DynamicResource StandardTextBox}" Width="120" Margin="0,0,10,0"/>
                <ComboBox Style="{DynamicResource StandardComboBox}" Width="120" SelectedIndex="0">
                    <ComboBoxItem Content="Option 1"/>
                    <ComboBoxItem Content="Option 2"/>
                </ComboBox>
            </StackPanel>
            
            <Label x:Name="InfoLabel" Content="Cliquez sur 'Test Standard' pour valider" Style="{DynamicResource FormLabel}" HorizontalAlignment="Center"/>
        </StackPanel>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CloseButton" Content="Fermer" Style="{DynamicResource StandardButton}"/>
        </StackPanel>
    </Grid>
</Window>